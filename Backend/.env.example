# 환경변수 설정 예시
# 실제 사용시 .env 파일로 복사하여 사용하세요

# Firebase 설정
FCM_CREDENTIALS_PATH=./firebase-credentials.json

# JWT 설정 (레거시 - Firebase 사용 권장)
JWT_SECRET=your-secret-key-change-this-in-production-minimum-256-bits

# 데이터베이스 설정
DATABASE_URL=jdbc:postgresql://localhost:5432/incheon_notice
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# 이메일 설정 (SMTP)
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# 프론트엔드 URL
FRONTEND_URL=http://localhost:3000

# 크롤링 서버 URL
CRAWLER_API_URL=http://localhost:8000

# 운영 환경 설정 (프로덕션에서만 사용)
# DATABASE_URL=jdbc:postgresql://your-prod-db:5432/incheon_notice
# DATABASE_USERNAME=your_prod_user
# DATABASE_PASSWORD=your_prod_password
# REDIS_HOST=your-prod-redis
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
